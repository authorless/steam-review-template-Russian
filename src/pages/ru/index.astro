---
import config_json from "../../review_templates/template_ru.json";
import PageLayout from "../../layouts/PageLayout.astro";
import OptionForm from "../../components/OptionForm.astro";
import FlagSelect from "../../components/FlagSelect.astro";
---

<PageLayout title="Шаблон обзора Steam">
  <header class="bg-slate-700 text-white py-2 flex flex-col items-center">
    <h1>{"---{ Шаблон обзора }---"}</h1>
    <FlagSelect />
    <p class="mt-0">☑ Отлично</p>
  </header>

  <div class="max-w-screen-sm mx-auto px-4">
    <p>
      Этот небольшой инструмент упрощает создание шаблонных обзоров для Steam (если
      вы на этом сайте — именно то, что вы ищете). Мне они кажутся довольно
      забавными, поэтому я сделал этот сайт, чтобы меньше копировать и вставлять
      одно и то же.&nbsp;😆
    </p>
    <p>
      Исходный код открыт &rarr; <a
        href="https://github.com/VojtaStruhar/steam-review-template"
      >
        смотрите на GitHub</a
      >!
    </p>
    <p>
      Результат генерируется со строкой внизу, которая даёт мне кредит.
      Оставлять её необязательно, но будет очень приятно! Спасибо!
    </p>
    {
      config_json.categories.map((category, index) => (
        <OptionForm category={category} index={index} />
      ))
    }
    <div class="flex justify-center">
      <button
        id="output-button"
        class="py-4 px-8 text-lg font-bold rounded-lg border-none shadow bg-amber-400 hover:cursor-pointer hover:shadow-lg transition-all"
      >
        Сгенерировать обзор Steam!</button
      >
    </div>

    <div class="flex justify-center mt-4">
      <button
        id="copy-output"
        class="py-2 px-4 text-lg rounded-lg border-none shadow bg-sky-100 hover:cursor-pointer hover:shadow-lg transition-all"
      >
        Скопировать результат</button
      >
    </div>
  </div>
</PageLayout>
